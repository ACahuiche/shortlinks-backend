{
  "name": "shortlinks",
  "version": "1.0.0",
  "description": "The backend of the shortlinks system",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "devstart": "npx nodemon --env-file .env app.js",
    "newstartenv": "node --watch --env-file .env app.js",
    "start": "node app.js"
  },
  "author": "ACahuiche",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.1.0",
    "morgan": "^1.10.0",
    "valid-url": "^1.0.9"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
